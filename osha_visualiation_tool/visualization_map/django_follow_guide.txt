django-admin.py startproject <<project name>>
django-admin.py startproject osha_visualiation_tool

cd osha_visualiation_tool/

python manage.py startapp <<app_name>>
python manage.py startapp visualization_map

<<project name>>/settings.py
geodjango/settings.py

##Configure settings.py

1. replace the default 
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
    }
}

2. use postgres with postgis

DATABASES = {
    'default': {
         'ENGINE': 'django.contrib.gis.db.backends.postgis',
         'NAME': 'geodjango',
         'USER': 'geo',
    },
}

or 

DATABASES = {
    'default': {
        'ENGINE':  'django.contrib.gis.db.backends.postgis',
        'NAME': 'osha_visualiation_tool', #<<project_name>>
        'USER': 'postgres',
        'PASSWORD': '',
        'HOST': 'localhost',
        'PORT': ''
    }
}


Update INSTALLED_APPS

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'django.contrib.gis', ##need for geographic data, admin rights
    'visualization_map'##<<app name>>
]



open up models.py in your <<app>> visulization_map folder
#replace
from django.db import models
with
from django.contrib.gis.db import models

IF your db is all ready created us the following command

python3 manage.py inspectdb > models_rev1.py



python3 manage.py makemigrations

python3 manage.py migrate


python3 manage.py runserver





